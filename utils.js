import fs from 'fs'

/**
 * mapTypes
 * 类型映射 
 */
export const mapTypes = (type) => {
    switch (type) {
        case '':
            return
        default:
            return
    }
}

/**
 * generateFile
 * 生成文件 
 */
export const generateFile = () => {}